{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": "allow"
  },
  "agent": {
    "build": {
      "command": {
        "run": "uv run",
        "install": "uv add",
        "remove": "uv remove",
        "sync": "uv sync"
      }
    }
  }
}
